<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-09-11 | Updated: 2026-09-11 -->

# vendor

## Purpose

Vendored-file directory currently containing only `.keep`. No vendored implementation is present in this checkout.

## Key Files

| File | Description |
|------|-------------|
| `.keep` | Empty file preserving this directory in Git. |

## For AI Agents

### Working In This Directory

Do not confuse this placeholder with an installed dependency bundle. `/vendor/bundle/` is ignored; project gem versions are declared in `Gemfile` and `Gemfile.lock`. Review provenance when intentionally adding vendored source.

### Testing Requirements

No executable implementation exists here. Documentation and placeholder edits need path/encoding checks; changes to the producing application configuration need its relevant tests.

## Dependencies

Root `.gitignore` and `.gitattributes`; Bundler manages gem dependencies.

<!-- MANUAL: Any manually added notes below this line are preserved on regeneration -->
