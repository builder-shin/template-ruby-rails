# Sidekiq-cron 스케줄 정의. config/initializers/sidekiq.rb 가 서버 부팅 시 로드합니다.
# 항목이 없으면(전부 주석) 아무 것도 등록되지 않습니다. 필요한 반복 작업을 추가하세요.
#
# 예시:
# cleanup_example:
#   cron: "0 3 * * * Asia/Seoul"
#   class: "YourRecurringJob"
#   queue: "default"
#   description: "매일 03:00 KST 실행되는 예시 작업"
