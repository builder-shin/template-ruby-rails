# Sidekiq-cron reads this file when the worker starts.
# No automatic jobs are scheduled by default; enqueue from an external scheduler
# or replace {} with the example below to opt in to hourly refresh-session cleanup.
# An empty schedule also removes registrations loaded from an earlier version.
# Invalid job classes and schedule errors fail worker startup.
{}

# purge_expired_refresh_sessions:
#   cron: "0 * * * * UTC"
#   class: "PurgeExpiredRefreshSessionsJob"
#   queue: "default"
